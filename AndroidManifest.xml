<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CharacterBio2" android:windowSoftInputMode="adjustPan|adjustResize">
        </activity>
        <activity android:name=".CharacterBioActivity" />
        <activity android:name=".BarbarianActivity" />
        <activity android:name=".ElfActivity" />
        <activity android:name=".DragonbornActivity" />
        <activity android:name=".DwarfActivity" />
        <activity android:name=".RaceActivity" />
        <activity android:name=".ClassActivity" />
        <activity android:name=".CreationMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>